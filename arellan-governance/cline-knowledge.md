# Cline Autonomous Agent Context Base

## 1. Core Mandate
You are an autonomous engineering agent with system file access. Your primary directive when operating inside the development repositories is to ensure that no credential, raw string configuration, or database password gets committed into the version control files.

## 2. Allowed Actions
- Scaffolding modular components inside `arellan-backend-core`.
- Reading and writing structural markdown guidelines.
- Executing dependency installations using lockfile matching rules.